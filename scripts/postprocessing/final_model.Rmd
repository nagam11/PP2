---
title: "Final Model"
author: "Michaela Müller"
date: "24 Januar 2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(data.table)
library(ggplot2)
library(cowplot)
```

## Bootstrapping Results
### Epochs
```{r}
epoch_train_acc <- fread("../validation/epoch_train_acc.tsv")
epoch_train_loss <- fread("../validation/epoch_train_losses.tsv")
epoch_val_acc <- fread("../validation/epoch_val_acc.tsv")
epoch_val_loss <- fread("../validation/epoch_val_losses.tsv")

epochs <- ncol(epoch_train_acc)
epoch_dt <- data.table(epoch = rep(1:epochs, 2),
                       acc = c(colMeans(epoch_train_acc), colMeans(epoch_val_acc)),
                       loss = c(colMeans(epoch_train_loss), colMeans(epoch_val_loss)),
                       data_set = rep(c("train", "validation"), each = epochs))
epoch_dt

ggplot(epoch_dt, aes(epoch, acc, col = data_set)) +
  geom_line() +
  scale_color_brewer(palette = "Set1")

ggplot(epoch_dt, aes(epoch, loss, col = data_set)) +
  geom_line() +
  scale_color_brewer(palette = "Set1")
```

### Test Statistics
```{r}
test_stats <- fread("../validation/test_stats.tsv", col.names = c("stat_name", "mean", "stderr", "confidence"))
test_stats

ggplot(test_stats, aes(stat_name, mean, fill = stat_name)) +
  geom_bar(stat = 'identity') + ylim(0, 1) +
  geom_errorbar(aes(ymin=mean-stderr, ymax=mean+stderr), width=.2) +
  scale_fill_brewer(palette = "Set1")
```

